Prefer using allowed tools in ~/.claude/settings.json
Avoid tools that are not auto-approved if convenient, if command is deemed safe suggest to the user that they should add it to the allow list
Prefer using right-sized subagents to keep current context down
